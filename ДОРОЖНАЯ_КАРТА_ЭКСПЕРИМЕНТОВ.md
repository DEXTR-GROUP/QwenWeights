# ДОРОЖНАЯ КАРТА ЭКСПЕРИМЕНТОВ LlamaWeights

## Назначение

Документ определяет последовательность независимого исследования реальных весов языковой модели.

Дорожная карта описывает исследовательские шаги и контрольные результаты. Она не фиксирует заранее конечную архитектуру и изменяется только на основании фактических результатов.

## Главный вопрос

Установить, из каких числовых структур и элементарных вычислений фактически состоит работа реальной языковой модели, и определить, до какой степени этот вычислительный механизм можно выразить в прозрачной форме и воспроизвести средствами Rust `no_std`.

## Исследовательская граница

```text
РЕАЛЬНЫЕ ВЕСА
      ↓
ФОРМАТ И СТРУКТУРА
      ↓
ЧИСЛОВЫЕ СВОЙСТВА
      ↓
ВЫЧИСЛИТЕЛЬНЫЕ ОПЕРАЦИИ
      ↓
ПРОМЕЖУТОЧНЫЕ СОСТОЯНИЯ
      ↓
ИТОГОВЫЙ РЕЗУЛЬТАТ
      ↓
МИНИМАЛЬНОЕ ПРЕДСТАВЛЕНИЕ
      ↓
RUST
      ↓
RUST no_std
```

## Этап W0. Фиксация контрольного набора

**Статус: ЗАКРЫТ.**

Зафиксирован первый контрольный объект:

```text
Модель: Qwen2.5-0.5B Base
Источник: Qwen/Qwen2.5-0.5B
Ревизия источника: 060db64
Основной файл: model.safetensors
Формат: Safetensors
Размер: около 988 MB
SHA-256: 88c142557820ccad55bb59756bfcfcf891de9cc6202816bd346445188a0ed342
Лицензия: Apache 2.0
```

**Результат:** контрольный набор однозначно идентифицирован.

## Этап W1. Получение и проверка весов

**Статус: ЗАКРЫТ.**

Получен точный набор файлов контрольного объекта. Проверены полнота набора, SHA-256 основного файла, целостность и соответствие контрольной идентификации.

**Результат закрытия:** локально полученный набор воспроизводимо распознаётся как зафиксированный контрольный объект и проходит проверку целостности.

## Этап W2. Карта весов

**Статус: ЗАКРЫТ.**

Построена и проверена структурная карта контрольных весов:

```text
модель
 ↓
слои
 ↓
тензоры
 ↓
размерности
 ↓
типы данных
 ↓
количество параметров
```

Фактический результат:

```text
Тензоров: 290
Параметров: 494,032,768
Dtype: BF16
Слоёв проверено: 24/24
Embedding shape: PASS
Attention projection shapes: PASS
MLP projection shapes: PASS
```

Подробное evidence: `results/W2_INVENTORY.md`.

**Результат:** получена подтверждённая структурная карта весов.

## Этап W3. Числовой профиль

**Статус: ТЕКУЩИЙ.**

Для выбранных тензоров исследовать:

- минимум и максимум;
- среднее;
- дисперсию;
- нормы;
- распределение знаков;
- распределение модулей;
- концентрацию около нуля;
- повторяющиеся и аномальные структуры;
- изменение характеристик между слоями.

**Результат закрытия:** получен воспроизводимый числовой профиль контрольного набора.

## Этап W4. Первый минимальный вычислительный участок

Выбрать один небольшой участок реального Transformer-контура и проследить его полностью:

```text
вход
 ↓
веса
 ↓
операции
 ↓
промежуточный результат
 ↓
следующая операция
```

Выбор участка определяется фактической структурой весов и тем, какой участок даст максимум понимания при минимальной сложности.

**Результат закрытия:** существует эталонный числовой трассировочный пример.

## Этап W5. Декомпозиция до примитивов

Для выбранного участка заменить высокоуровневые операции последовательностью простых операций и определить минимальный необходимый набор.

Исследуемые кандидаты:

- чтение;
- запись;
- умножение;
- сложение;
- накопление;
- деление или эквивалентная операция;
- нормализация;
- нелинейное преобразование;
- индексация;
- сравнение и выбор.

Набор не считается окончательным, пока не подтверждён экспериментом.

**Результат закрытия:** вычисление представлено через явно подтверждённую последовательность примитивов.

## Этап W6. Воздействие весов

Проводить контролируемые изменения параметров, если они необходимы для ответа на исследовательский вопрос:

```text
W' = W
W'[i] = 0
```

```text
W' = kW
```

Измерять:

```text
ΔW → Δпромежуточное состояние → Δвыход
```

**Результат закрытия:** установлены количественные зависимости для выбранного участка.

## Этап W7. Независимый numerical oracle

Создать независимую реализацию выбранного участка, не использующую исследуемую реализацию как единственный источник истины.

Сравнивать:

- промежуточные значения;
- итоговые значения;
- допустимую численную погрешность;
- граничные случаи.

**Результат закрытия:** получено независимое подтверждение вычислений.

## Этап W8. Реализация на Rust

Перенести выбранный примитивный участок в Rust.

Цель:

```text
эталон
   ==
Rust
```

с измеряемой погрешностью, если арифметика не является битово идентичной.

**Результат закрытия:** Rust-вариант воспроизводит контрольный участок.

## Этап W9. Реализация на Rust `no_std`

Убрать зависимость от стандартной библиотеки для выбранного участка и проверить:

- фиксированное представление памяти;
- отсутствие динамических аллокаций в исследуемом ядре;
- детерминированность;
- воспроизводимость результатов.

**Результат закрытия:** выбранный вычислительный участок выполняется в `no_std`-контуре.

## Этап W10. Сравнение эффективности

Сравнить эталонный и исследуемые варианты по измеряемым характеристикам:

```text
время
память
число операций
обращения к памяти
числовая ошибка
детерминированность
```

Не считать вариант более эффективным без фактического benchmark и контрольного сравнения.

**Результат закрытия:** сформирован количественный отчёт об эффективности конкретного участка.

## Этап W11. Масштабирование

Только после успешного завершения W10 расширять исследование:

```text
1 операция
 ↓
1 подблок
 ↓
1 Transformer block
 ↓
несколько блоков
```

Каждый переход должен сохранять независимый контрольный результат.

**Результат:** установлена граница масштабируемости исследовательского подхода.

## Этап W12. Следующий объект исследования

Не назначается заранее.

После завершения текущего исследования решение о другом участке, другой модели или другом представлении принимается только на основании фактических результатов W1–W10.

До этого этап не активируется.

## Правила закрытия этапов

1. Нет измерения — нет утверждения об эффективности.
2. Нет контрольного результата — нет закрытия этапа.
3. Любой существенный вывод должен быть воспроизводим.
4. Изменение весов и изменение вычислительного алгоритма должны рассматриваться раздельно.
5. Результат одного этапа не считается доказательством для всей модели.
6. Не допускается подмена фактического эксперимента архитектурной гипотезой.
7. Не выбирать следующий объект исследования до получения достаточного результата по текущему объекту.

## Текущая контрольная точка

**W3: получить воспроизводимый числовой профиль контрольного набора Qwen2.5-0.5B Base.**

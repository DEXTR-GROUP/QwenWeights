# LlamaWeights

## Назначение

`LlamaWeights` — отдельное закрытое исследовательское хранилище весов языковых моделей и материалов, необходимых для их независимого численного исследования.

Репозиторий не является частью `LLAMA-LAB`, `UDEKERNEL`, `NS-AI` или другого проекта. Его назначение — исследовать веса как числовую структуру и установить, как они участвуют в вычислительном процессе модели.

Главный исследовательский вопрос:

> Можно ли представить вычислительный механизм, реализуемый реальными весами языковой модели, через минимальный набор прозрачных числовых примитивов и затем воспроизвести его в Rust `no_std`?

## Принцип разделения

Исследование разделяет:

```text
АРХИТЕКТУРА
    ↓
ВЫЧИСЛИТЕЛЬНЫЕ ОПЕРАЦИИ
    ↓
ВЕСА
    ↓
ПРОМЕЖУТОЧНЫЕ СОСТОЯНИЯ
    ↓
РЕЗУЛЬТАТ
```

Вес не рассматривается как самостоятельная «способность». Он изучается только в связи с конкретным вычислительным контуром и измеряемым результатом.

## Первый контрольный объект

**Qwen2.5-0.5B Base**.

Официальный источник: `Qwen/Qwen2.5-0.5B`.

Выбор сделан не по качеству генерации, а по исследовательской доступности: небольшой реальный Transformer, открытые веса, понятная конфигурация и возможность начать анализ без gated-доступа.

На первом этапе исследуется именно **Base**, а не Instruct-вариант. Нам требуется максимально чистое исследование базового вычислительного механизма без дополнительной задачи инструкционного поведения.

Точная версия, файлы, размеры, контрольные суммы и фактическая структура фиксируются после загрузки. До этой проверки набор весов не считается принятым.

## Что исследуется

### 1. Числовая структура

- типы чисел;
- диапазоны значений;
- распределения;
- средние значения;
- дисперсии;
- нормы;
- экстремальные значения;
- плотность около нуля;
- различия между слоями и группами параметров.

### 2. Примитивные операции

Исследуется возможность разложения высокоуровневого вычисления на базовые операции:

```text
загрузка
умножение
сложение
накопление
нормализация
нелинейное преобразование
индексация
запись результата
```

Конкретный минимальный набор должен быть установлен экспериментально.

### 3. Чувствительность к весам

Для контролируемых экспериментов используются изменения вида:

```text
W' = W
W'[i] = 0
```

или масштабирование:

```text
W' = kW
```

Затем измеряется изменение промежуточных значений и конечного результата.

### 4. Воспроизводимость

Для каждого значимого эксперимента должны существовать:

- фиксированный вход;
- фиксированная версия весов;
- фиксированная конфигурация вычисления;
- эталонный результат;
- независимая проверка;
- описание измеряемой величины.

### 5. Перенос в Rust `no_std`

После восстановления конкретного вычислительного участка проверяется возможность его реализации без стандартной библиотеки:

```text
reference
    ↓
primitive numerical form
    ↓
Rust
    ↓
Rust no_std
```

Цель — не сделать преждевременный полный порт модели, а установить минимальные требования конкретного вычислительного участка.

## Что не является целью

- обучение новой модели;
- создание собственного сервиса генерации текста;
- оптимизация ради одного только benchmark-показателя;
- немедленный полный порт всей модели в `no_std`;
- включение весов или результатов этого исследования в другие проекты без отдельного решения;
- утверждения о «смысле» весов без воспроизводимого численного свидетельства.

## Правило исследования

**Сначала воспроизвести. Затем понять. Затем упростить. Затем оптимизировать.**

Нельзя объявлять вычисление упрощённым, эквивалентным или более эффективным без измерительного подтверждения.

## Связь с внешними проектами

По умолчанию связь отсутствует.

Любая передача результатов в другой проект должна быть отдельным осознанным решением и фиксироваться как новая исследовательская связь.

## Текущее состояние

Репозиторий создан как отдельная исследовательская сущность.

Первый контрольный объект выбран: **Qwen2.5-0.5B Base**.

Текущая рабочая граница — W0/W1: получить точный набор весов, зафиксировать его происхождение и проверить целостность. Дальнейшее исследование начинается только после фактического подтверждения этого набора.
